/* Arrays and objects

const colors = ['red', 'green', 'blue']; 

TODO: Create an object that maps color names to their hexadecimal values 
*/
const colorMap = {
  red: '#FF0000',
  green: '#00FF00',
  blue: '#0000FF'
};

console.log(colorMap);
